# Prisma (SQLite) — サーバ保存を有効にする場合のみ 1 に設定
SERVER_PERSISTENCE=0
DATABASE_URL="file:./data.db"
# Example environment variables
# NEXT_PUBLIC_ variables are exposed to the browser at build time.

# Set to 1 to route STT via Next.js API (avoids CORS)
NEXT_PUBLIC_STT_VIA_PROXY=0

# Enable server persistence APIs (self-hosted only)
# SERVER_PERSISTENCE=1
